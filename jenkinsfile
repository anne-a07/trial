pipeline {
    agent any
    
    stages {
        stage('Login and Push') {
            steps {
                script {
                    withDockerRegistry(credentialsID: 'Docker') {
                        docker.build('anne07/flask').push('latest')
                    }
                }
            }
        }
    }
}